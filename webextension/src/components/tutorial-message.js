import { h } from 'preact'

const tutorialMessage = () => (
	<p className="tutorial-msg">
		To fetch your bookmarks for use in Bukubrow click the button with the arrow in it above.<br />Do this whenever you want to refresh your local cache of bookmarks with those from Buku.
	</p>
)

export default tutorialMessage
