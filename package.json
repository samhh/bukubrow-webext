{
    "scripts": {
        "assets": "cp static/* dist/",
        "dev": "yarn run assets && parcel watch entry/content.html entry/options.html entry/backend.js",
        "build": "yarn run assets && parcel build entry/content.html entry/options.html entry/backend.js"
    },
    "dependencies": {
        "webextension-polyfill-ts": "^0.12.0"
    },
    "devDependencies": {
        "parcel": "^2.0.0-alpha.3.2"
    }
}
