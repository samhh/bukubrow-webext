{
    "scripts": {
        "assets": "cp static/* dist/",
        "bundle-dev": "yarn run assets && parcel watch entry/content.html entry/options.html entry/backend.js",
        "bundle-prod": "yarn run assets && parcel build entry/content.html entry/options.html entry/backend.js"
    },
    "dependencies": {
        "uuid": "^3.4.0",
        "uuid-validate": "^0.0.3",
        "webextension-polyfill-ts": "^0.12.0"
    },
    "devDependencies": {
        "parcel": "^2.0.0-alpha.3.2"
    }
}
